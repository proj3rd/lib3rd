// In case of section information is not contained in heading tag (h1-h6)
// Supports form of X.Y.Z and X.Y.Za
export const reSection = /^\d+(\.\d+)*?\.\d+\w?\s+?.+$/;
export const reReference = /\d+(\.\d+)*?\.\d+\w?/;
